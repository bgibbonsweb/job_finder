{
  "name": "job_finder",
  "version": "1.0.0",
  "description": "Basic Node.js server",
  "main": "server.js",
  "scripts": {
    "build": "npm --prefix frontend install --include=dev && npm --prefix frontend run build",
    "start": "node --max-old-space-size=1200 server.js",
    "dev": "node --watch server.js",
    "test": "node --test 'test/**/*.test.js'",
    "dev:frontend": "npm --prefix frontend run dev",
    "go": "concurrently --kill-others --prefix-colors cyan,green --names server,frontend \"npm run dev\" \"npm run dev:frontend\"",
    "db:init": "node server/db/init.js",
    "db:migrate": "node server/db/migrate.js"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "busboy": "^1.6.0",
    "docx": "^9.6.1",
    "dotenv": "^16.0.3",
    "pdf-parse": "^2.4.5",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
